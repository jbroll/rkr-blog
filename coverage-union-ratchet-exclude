# Files the union coverage ratchet must not gate. One path per line;
# blank lines and # comments ignored. See docs/TESTING.md §10.
#
# The only admissible reason to be on this list is that the file emits no
# JavaScript at all, so it can never appear in an lcov. The ratchet already
# special-cases .d.ts; these are .ts files that are equally type-only, each
# verified with `npx esbuild <file> --format=esm` producing empty output.
# "No test covers it yet" is not a reason — write the test.
packages/image-edit/src/core/sidecar-types.ts
src/admin/opfs-worker-msg.ts
src/lib/wp-import-types.ts
src/lib/image-map.ts
